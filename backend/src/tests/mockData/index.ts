import { validUsers, invalidUsers } from "./users";
import { validMeetings, invalidMeetings } from "./meetings";
import { validPolls, invalidPolls } from "./polls";
import { validRequests, invalidRequests } from "./requests";

export default {
  validUsers,
  invalidUsers,
  validMeetings,
  invalidMeetings,
  validPolls,
  validRequests,
  invalidRequests,
  invalidPolls,
}
